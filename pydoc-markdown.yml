# pydoc-markdown.yml (Test multiple boolean keys and pages_type: MODULE)
loaders:
  - type: python
    search_path: [src] # Ensure src/ has your test.py with docstrings

processors:
  - type: smart

renderer:
  type: markdown
  render_module_header: false # This was previously accepted
  render_toc: false           # Testing another boolean (default is true)
  descriptive_toc: true       # Testing another boolean (default is false)
  pages_type: MODULE          # Still trying to get this Enum recognized
  # output_directory: "pydoc_generated_docs" # Keep the main problem child commented for this test